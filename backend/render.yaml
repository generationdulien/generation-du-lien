services:
  - type: web
    name: generation-lien-api
    runtime: node
    rootDir: backend
    buildCommand: npm ci && npm run build
    startCommand: node dist/main.js
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: development
      - key: LOG_LEVEL
        value: info
